# Cloudflare Pages - deploy with: npx wrangler pages deploy dist
# Or connect repo at https://dash.cloudflare.com/pages

name = "geneai"
compatibility_date = "2024-01-01"

# For Cloudflare Pages, use: wrangler pages project create geneai
# Then: wrangler pages deploy dist --project-name geneai

[env.production]
vars = { ENVIRONMENT = "production" }
