# Cloudflare Pages - deploy with: npx wrangler pages deploy dist
# Or connect repo at https://dash.cloudflare.com/pages

name = "pharmagen"
compatibility_date = "2024-01-01"

# For Cloudflare Pages, use: wrangler pages project create pharmagen
# Then: wrangler pages deploy dist --project-name pharmagen

[env.production]
vars = { ENVIRONMENT = "production" }
